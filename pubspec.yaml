name: ditonton
description: Main package for ditonton app.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  about:
    path: about
  core:
    path: core
  movies:
    path: movies
  tv_shows:
    path: tv_shows

  http: ^0.13.3
  provider: ^5.0.0
  get_it: ^7.1.3
  firebase_core: ^1.6.0
  firebase_crashlytics: ^2.2.4

dev_dependencies:
  mockito: ^5.0.8
  build_runner: ^2.0.4
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - certificates/
